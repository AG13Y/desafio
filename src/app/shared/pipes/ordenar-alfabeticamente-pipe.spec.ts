import { OrdenarAlfabeticamentePipe } from './ordenar-alfabeticamente-pipe';

describe('OrdenarAlfabeticamentePipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarAlfabeticamentePipe();
    expect(pipe).toBeTruthy();
  });
});
